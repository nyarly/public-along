The table below shows changes made to employee information in the last 24 hours.

+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
|                                                                                    Job Changes                                                                                     |
+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| Parent Org   | Department   | Employee ID   | Name     | Job Title   | Manager Name   | Location   | Start Date   | Change Type | Old Value | New Value | Changed At | Worker Type |
+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
<% @job_changes.each do |delta| %>
<% @changes = delta.format_by_key %>
<% @changes.each do |change| %>
|<%= delta.employee.department.parent_org.try(:name) %> | <%= delta.employee.department.try(:name) %> | <%= delta.employee.current_profile.adp_employee_id %> | <%= delta.employee.cn %> | <%= delta.employee.job_title.try(:name) %> | <%= delta.employee.manager.try(:cn) %> | <%= delta.employee.location.try(:name) %> | <%= l delta.employee.current_profile.start_date, format: :mdy %> | <%= change["name"].titleize %> | <%= change["before"] %> | <%= change["after"] %> | <%= l delta.created_at, format: :mdyhm %> | <%= delta.employee.worker_type.try(:name) %> |
+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
<% end %>
<% end %>

Attached you will find a list of employees and contingent workers that have had job changes within the last 48 hours. If you have any questions, please e-mail peopleandculture@opentable.com.
