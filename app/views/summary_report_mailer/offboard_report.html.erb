<style type="text/css">
  html {
    font-family: helvetica;
  }
  table, th, td {
    border: 1px solid black;
  }
  table {
    border-collapse: collapse;
  }
  th, td {
    padding: .25em;
  }
</style>
<div style="font-family: helvetica; margin: 20px; width: 500px">
  <p>Hello,</p>
  <p>
    The table below shows workers offboarded by Mezzo since the last email. The attachment contains a list of employees and contingent workers offboarded within the last week. If you have any questions, please e-mail <%= mail_to "peopleandculture@opentable.com." %>
  </p>
</div>
<div style="margin: 20px;">
  <table>
    <thead>
      <tr>
        <th>Parent Org</th>
        <th>Department</th>
        <th>Worker FName</th>
        <th>Worker LName</th>
        <th>Worker OT Email</th>
        <th>Worker Category Type</th>
        <th>Location</th>
        <th>Termination Date</th>
        <th>Manager FName</th>
        <th>Manager LName</th>
      </tr>
    </thead>
    <tbody>
      <% @offboards.each do |offboard| %>
        <tr>
          <td><%= offboard.employee.department.parent_org.try(:name) %></td>
          <td><%= offboard.employee.department.try(:name) %></td>
          <td><%= offboard.employee.first_name %></td>
          <td><%= offboard.employee.last_name %></td>
          <td><%= offboard.employee.email %></td>
          <td><%= offboard.employee.worker_type.try(:name) %></td>
          <td><%= offboard.employee.location.try(:name) %></td>
          <td><%= offboard.employee.termination_date.try(:strftime, "%b %e, %Y") %></td>
          <td><%= offboard.employee.manager.try(:first_name) %></td>
          <td><%= offboard.employee.manager.try(:last_name) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <% if @offboards.size == 0 %>
    <p>There were no new offboards since the last report.</p>
  <% end %>
<div>
<%= image_tag attachments['pandc.png'].url -%>
