You have an employee event form awaiting your action for:
=======================================

Name: <%= @employee.cn %>

Title: <%= @employee.job_title.try(:name) %>

Department: <%= @employee.department.name %>

Location: <%= @employee.location.name %>

=======================================
<% if @kind == 'security_access' %>
<% if @employee.emp_deltas.important_changes.present? %>
You are receiving this form because you are listed as the manager of <%= @employee.cn %>, who may need a security profile update because the following information changed on <%= l @employee.emp_deltas.important_changes.last.created_at, format: :verbose %>:
<% @employee.emp_deltas.important_changes.last.format_by_key.each do |e| %>
  <%= e['name'] %> changed from <%= e['before'] %> to <%= e['after'] %>
<% end %>
<% end %>
If you do not wish to make any changes, you may disregard this email.
<% elsif @kind == "onboarding" || @kind == 'job_change' %>
You must complete this form by <%= l @employee.onboarding_due_date, format: :verbose %>. The onboarding form is necessary to have your new hire’s equipment and access ready for their first day. Please fill it out as soon as possible so that we can provide the best possible onboarding experience.
<% elsif @kind == "offboarding" %>
<% if @employee.needs_contract_end_confirmation? %>
This worker's contract expires on <%= @employee.contract_end_date.strftime("%b %e, %Y") %>.

* If you wish to extend their contract: please fill out the P&C contractor request form here: https://docs.google.com/forms/d/1sFNGn6hW3StGJJFjH5zT2-QKR-Y7xrNXQ4w1zHH5KuA

* If the contractor's last day is listed correctly, you should fill out the <%= link_to "offboarding form", new_emp_transaction_url({
:employee => @employee.id,
:kind => @kind}) %> by noon on <%= @employee.contract_end_date.strftime("%b %e, %Y") %>
<% else %>
You must complete this form by noon on <%= @employee.termination_date.strftime("%b %e, %Y") %>.
<% end %>
------------------------------------------------------
<% end %>


Please note you must be logged into the OpenTable VPN to access Mezzo. Use your OpenTable user name and password to log in. Follow the link below to complete the employee event form:
<% if @event.present? %>
<%= link_to "Employee Event Form for #{@employee.cn}", new_emp_transaction_url({
  :event_id => @event.id,
  :kind => @kind}) %>
<% else %>
<%= new_emp_transaction_url({
    :employee => @employee.id,
    :kind => @kind}) %>
<% end %>

For any questions, please contact othelpme@opentable.com.
