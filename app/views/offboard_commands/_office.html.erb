<pre>
$CRED = Get-Credential
$SESSION = New-PSSession -ConfigurationName Microsoft.Exchange -ConnectionUri https://ps.outlook.com/powershell -Credential $CRED -Authentication Basic -AllowRedirection
Import-PSSession $SESSION
Connect-MsolService -Credential $CRED
Get-Mailbox -identity <%= @offboard_info.employee_email %> | set-mailbox -type "Shared"
Set-Mailbox -identity <%= @offboard_info.employee_email %> -DeliverToMailboxAndForward $true -ForwardingAddress <%= @offboard_info.forward_email.email %>
Set-MsolUserLicense -UserPrincipalName <%= @offboard_info.employee_email %> -RemoveLicenses "opentable:ENTERPRISEPACK"
Add-MailboxPermission <%= @offboard_info.employee_email %> -User "<%= @offboard_info.forward_email.email %>" -AccessRights FullAccess
</pre>
