<pre>
Enable-ADAccount -Identity <%= @employee.sam_account_name %>
$CRED = Get-Credential
$SESSION = New-PSSession -ConfigurationName Microsoft.Exchange -ConnectionUri https://ps.outlook.com/powershell -Credential $CRED -Authentication Basic -AllowRedirection
Import-PSSession $SESSION
Connect-MsolService -Credential $CRED
Set-MsolUser -UserPrincipalName <%= @employee.email %> -UsageLocation US
Set-MsolUserLicense -UserPrincipalName <%= @employee.email %> -AddLicenses "opentable:ENTERPRISEPACK"
$LO = New-MsolLicenseOptions -AccountSkuId "opentable:ENTERPRISEPACK" -DisabledPlans "MCOSTANDARD", "TEAMS1"
Set-MsolUserLicense -UserPrincipalName <%= @employee.email %> -LicenseOptions $LO
Enable-RemoteMailbox <%= @employee.email %> –RemoteRoutingAddress <%= @employee.sam_account_name %>@opentable.mail.onmicrosoft.com
</pre>
