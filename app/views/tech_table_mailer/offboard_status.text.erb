Mezzo Automatic Offboarding Status
========================

Termination Date: <%= @employee.termination_date.strftime("%b %e, %Y") %>

Name: <%= @employee.cn %>

Email: <%= @employee.email %>

EmployeeID: <%= @employee.employee_id %>

Automatic Account Offborading Status:
---------------------------------
<% @employee.emp_access_levels.each do |al| %>
  <%= AccessLevel.find(al.access_level_id).name %>
  Active: <%= al.active %>
<% end %>
