<%= form_for(@worker_type, :html => {:class => "form"}) do |f| %>
  <% if @worker_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worker_type.errors.count, "error") %> prohibited this worker_type from being saved:</h2>

      <ul>
      <% @worker_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :kind %><br>
    <%= f.select :kind, WorkerType::KINDS %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.select :status, WorkerType::STATUS %>
  </div>
  <div class="actions">
    <%= link_to 'Back to List', worker_types_path, :class => "button secondary" %>
    <%= f.submit "Submit", :class => "button"%>
  </div>
<% end %>
