<%= form_for(@parent_org, :html => {:class => "form"} ) do |f| %>
  <% if @parent_org.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent_org.errors.count, "error") %> prohibited this parent_org from being saved:</h2>

      <ul>
      <% @parent_org.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= link_to 'Back to List', parent_orgs_path, :class => "button secondary" %>
    <%= f.submit "Submit", :class => "button"%>
  </div>
<% end %>
